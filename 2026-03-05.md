**Let function**﻿
To handle NULL values and perform operations only on non-null types, you can use the safe call operator ?. together with the let function.

val name: String? = "John"
name?.let {
println("Name length: ${it.length}")
}

?. checks if name is not null
If not null → let executes
it represents the object (name)
Related Nullable Collection Types
   Type 
Meaning
List<String?>
  List can contain null elements
List<String>?
List itself may be null
List<String?>?
List may be null and elements may be null



fun main(args:Array<String>){
     var listWithNulls: List<String?> =listOf("Kotlin",null)
   for(item in listWithNulls){
   Item?.let{
     println(it)
}
  }


**Safe casts**﻿
The regular Kotlin operator for type casts is the as operator. However, regular casts can result in an exception if the object is not of the target type.
You can use the as? operator for safe casts. It tries to cast a value to the specified type and returns null if the value is not of that type:

fun main(args:Array<String>){
  val a:String="Hello,Kotlin"
    val aInt = a as?  Int
    val aString = a as? String
   println(aInt)//null
    println(aString)//”Heelo,Kotlin”
}

The code above prints null because a is not an Int, so the cast fails safely. It also prints "Hello, Kotlin!" because it matches the String? type, so the safe cast succeeds.
Collections of a nullable type

If you have a collection of nullable elements and want to keep only the non-null ones, use the filterNotNull() function:
 
 fun main(args:Array<String>){
val nullableList: List<Int?> = listOf(1, 2, null, 4)
val intList: List<Int> = nullableList.filterNotNull()
println(intList)
// [1, 2, 4]
 }    
}


Link:NULL SAFETY ::https://pl.kotl.in/6PghXWYBv


**FUNCTION AS EXPRESSION**

fun main(){
var largeValue=max(4,6)
}
fun max(a:Int,b:Int):Int =if(a>b) a else b



**Interoperability**

fun main(){
Var area=MyJavaFile.getArea(3,5)
println(area)
}
fun add(a:Int,b:Int):Int{
return a+b
}
Public class MyJavaFile{
Public static void main(String[] args){
Int sum=MyfirstKt.add(3,4);
System.out.println(“Printing sum from java file” +sum);
}
Public static int getArea(int l,int b){
Return l*b;
}
}
